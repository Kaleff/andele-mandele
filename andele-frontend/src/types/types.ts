export type RouterError = {
  statusText: string;
  message: string;
}

export type PaginationEvent = {
  selected: number;
}

export type EpisodeType = {
  name: string;
  air_date: string;
  episode: string;
}